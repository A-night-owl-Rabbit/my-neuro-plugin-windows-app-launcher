{
  "name": "windows-app-launcher-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for windows-app-launcher plugin",
  "dependencies": {
    "iconv-lite": "^0.7.0"
  }
}
